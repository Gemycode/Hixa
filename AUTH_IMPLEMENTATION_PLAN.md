# خطة تنفيذ نظام Login و Register الكامل

## 📋 تحليل الوضع الحالي

### ✅ ما هو موجود حالياً:
1. **نظام أساسي للتسجيل والدخول** (`/register`, `/login`)
2. **نموذج المستخدم** يحتوي على:
   - الحقول الأساسية: email, password, name, role
   - حقول إضافية: phone, nationalId, location, bio, specializations, certifications
   - الأدوار المتاحة: `admin`, `engineer`, `client`, `customer`
3. **التحقق من البيانات** (Validation) أساسي
4. **JWT Token** للتوثيق

### ❌ ما هو ناقص:

#### 1. **نقاط نهاية (Endpoints) محددة لكل دور:**
   - ❌ `/auth/register/company` - تسجيل الشركات
   - ❌ `/auth/register/engineer` - تسجيل المهندسين
   - ❌ `/auth/register/client` - تسجيل العملاء
   - ❌ `/auth/register/admin` - تسجيل الأدمن (إن وجد)

#### 2. **حقول محددة لكل دور:**

   **Company (الشركة):**
   - ❌ Company Name (اسم الشركة)
   - ❌ Contact Person Name (اسم الشخص المسؤول)
   - ✅ Email
   - ✅ Password
   - ❌ Confirm Password (تأكيد كلمة المرور)

   **Engineer (المهندس):**
   - ✅ Full Name (الاسم الكامل) - موجود كـ `name`
   - ❌ Specialization (التخصص)
   - ❌ License Number (رقم الرخصة)
   - ✅ Email
   - ✅ Password
   - ❌ Confirm Password

   **Client (العميل):**
   - ✅ Full Name (الاسم الكامل) - موجود كـ `name`
   - ✅ Email
   - ✅ Password
   - ❌ Confirm Password

#### 3. **التحقق من البيانات (Validation):**
   - ❌ التحقق من تطابق كلمة المرور (Confirm Password)
   - ❌ Validation مخصص لكل دور
   - ❌ التحقق من الحقول المطلوبة لكل دور

#### 4. **نموذج البيانات (User Model):**
   - ❌ إضافة دور "company" إذا كان مطلوباً (حالياً: admin, engineer, client, customer)
   - ❌ إضافة حقل `companyName` للشركات
   - ❌ إضافة حقل `contactPersonName` للشركات
   - ❌ إضافة حقل `licenseNumber` للمهندسين
   - ✅ `specializations` موجود (للمهندسين)

---

## 🎯 خطة التنفيذ - الخطوات

### **المرحلة 1: تحديث نموذج المستخدم (User Model)**

#### الخطوة 1.1: إضافة دور "company" (إن لم يكن موجوداً)
- تحديث `enum` في `role` لتشمل `"company"`

#### الخطوة 1.2: إضافة الحقول الجديدة
- `companyName` (String, optional) - لشركات
- `contactPersonName` (String, optional) - لشركات  
- `licenseNumber` (String, optional, unique, sparse) - للمهندسين

---

### **المرحلة 2: إنشاء Validation Schemas مخصصة**

#### الخطوة 2.1: إضافة Validation للتسجيل حسب الدور
- `validateCompanyRegister` - للشركات
- `validateEngineerRegister` - للمهندسين
- `validateClientRegister` - للعملاء
- `validateAdminRegister` - للأدمن (إن لزم)

#### الخطوة 2.2: إضافة التحقق من Confirm Password
- إضافة حقل `confirmPassword` في جميع schemas
- التحقق من تطابق `password` و `confirmPassword`

---

### **المرحلة 3: تحديث Auth Controller**

#### الخطوة 3.1: إنشاء Controllers مخصصة لكل دور
- `registerCompany` - تسجيل شركة
- `registerEngineer` - تسجيل مهندس
- `registerClient` - تسجيل عميل
- `registerAdmin` - تسجيل أدمن (إن لزم)

#### الخطوة 3.2: تحديث Login
- التأكد من أن Login يعمل مع جميع الأدوار
- إرجاع البيانات المطلوبة حسب الدور

---

### **المرحلة 4: تحديث Routes**

#### الخطوة 4.1: إضافة Routes مخصصة
- `POST /auth/register/company`
- `POST /auth/register/engineer`
- `POST /auth/register/client`
- `POST /auth/register/admin` (إن لزم)

#### الخطوة 4.2: الحفاظ على Route العام
- `POST /auth/register` - يبقى للاستخدام العام (اختياري)

---

### **المرحلة 5: الاختبار**

#### الخطوة 5.1: اختبار كل endpoint
- اختبار تسجيل شركة
- اختبار تسجيل مهندس
- اختبار تسجيل عميل
- اختبار تسجيل أدمن (إن لزم)
- اختبار Login لكل دور

#### الخطوة 5.2: اختبار Validation
- اختبار الحقول المطلوبة
- اختبار تطابق كلمة المرور
- اختبار صحة البيانات

---

## 📝 تفاصيل الحقول المطلوبة لكل دور

### **Company Registration:**
```json
{
  "companyName": "اسم الشركة",
  "contactPersonName": "اسم الشخص المسؤول",
  "email": "email@example.com",
  "password": "Password123",
  "confirmPassword": "Password123"
}
```

### **Engineer Registration:**
```json
{
  "fullName": "الاسم الكامل",
  "specialization": "Civil Engineer, Electrical Engineer",
  "licenseNumber": "رقم الرخصة المهنية",
  "email": "email@example.com",
  "password": "Password123",
  "confirmPassword": "Password123"
}
```

### **Client Registration:**
```json
{
  "fullName": "الاسم الكامل",
  "email": "email@example.com",
  "password": "Password123",
  "confirmPassword": "Password123"
}
```

---

## 🔧 الملفات التي سيتم تعديلها/إنشاؤها

### **ملفات موجودة سيتم تعديلها:**
1. `models/userModel.js` - إضافة حقول جديدة
2. `controllers/authController.js` - إضافة controllers جديدة
3. `middleware/validate.js` - إضافة validation schemas جديدة
4. `routes/authRoutes.js` - إضافة routes جديدة

### **ملفات جديدة (إن لزم):**
- لا توجد ملفات جديدة مطلوبة

---

## ⚠️ ملاحظات مهمة

1. **دور "company":** يجب التأكد إذا كان مطلوباً إضافة دور جديد أم استخدام `client` للشركات
2. **Confirm Password:** لن يتم حفظه في قاعدة البيانات، فقط للتحقق
3. **License Number:** يجب أن يكون unique للمهندسين (sparse index)
4. **Security:** التأكد من تطبيق نفس مستوى الأمان على جميع endpoints
5. **Rate Limiting:** تطبيق rate limiting على جميع endpoints الجديدة

---

## ✅ بعد التنفيذ

بعد إكمال جميع الخطوات، سيكون لدينا:
- ✅ نظام تسجيل كامل لكل دور
- ✅ Validation مخصص لكل دور
- ✅ Confirm Password validation
- ✅ Routes منظمة وواضحة
- ✅ نموذج بيانات محدث

---

## 📅 الترتيب المقترح للتنفيذ

1. **أولاً:** تحديث User Model
2. **ثانياً:** إضافة Validation Schemas
3. **ثالثاً:** تحديث Auth Controller
4. **رابعاً:** تحديث Routes
5. **خامساً:** الاختبار

---

**جاهز للتنفيذ؟** ✅


